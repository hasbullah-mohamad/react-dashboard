import {
  login, 
  logout, 
  authHeader, 
  getCallbackUri, 
  isLoggedIn, 
  scheduleRenewal, 
  setAccessToken, 
  setExpiresAt,
  setStayLoggedIn,
  getStayLoggedIn
} from './Auth'

export default {
  login, 
  logout, 
  authHeader, 
  getCallbackUri, 
  isLoggedIn, 
  scheduleRenewal, 
  setAccessToken, 
  setExpiresAt,
  setStayLoggedIn,
  getStayLoggedIn
}
